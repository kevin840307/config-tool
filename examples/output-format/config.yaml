version: 1
options:
  yaml_output:
    mapping: 2
    sequence: 4
    offset: 2
    width: 120
    preserve_quotes: true
operations:
  - op: append
    path: $.services
    value:
      name: worker
      enabled: true
